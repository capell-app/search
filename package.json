{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "node build.mjs",
        "build:check": "node build.mjs --check"
    },
    "devDependencies": {
        "esbuild": "^0.28.1",
        "prettier": "3.9.4"
    }
}
